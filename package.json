{
  "name": "luajs",
  "version": "1.0.0",
  "description": "Bindings for C Lua to JavaScript with Emscripten and WebAssembly",
  "main": "index.js",
  "scripts": {
    "clean": "emmake make clean",
    "build": "tsc && emmake make install",
    "lint": "echo 'Lint not set up yet'",
    "test": "npm run build && node --test"
  },
  "author": "Doridian",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.2"
  }
}
