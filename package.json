{
  "name": "luajs",
  "version": "1.0.0",
  "description": "Bindings for C Lua to JavaScript with Emscripten and WebAssembly",
  "main": "util/loader.mjs",
  "type": "module",
  "scripts": {
    "clean": "emmake make clean",
    "build": "mkdir -p dist && cp -f src/luajs.d.mts dist/ && cd src/ && tsc && cd .. && tsc && emmake make install",
    "lint": "echo 'Lint not set up yet'",
    "fmt": "echo 'Format not set up yet'",
    "test": "npm run build && npm run testOnly",
    "testOnly": "node ./util/testrunner.mjs"
  },
  "author": "Doridian",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.2",
    "glob": "^10.3.10",
    "typescript": "^5.3.2"
  }
}
