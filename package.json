{
  "name": "luajs",
  "version": "1.0.0",
  "description": "Bindings for C Lua to JavaScript with Emscripten and WebAssembly",
  "main": "index.js",
  "scripts": {
    "clean": "emmake make clean",
    "build": "mkdir dist && cp src/luajs.d.ts dist/ && tsc && emmake make install",
    "lint": "echo 'Lint not set up yet'",
    "fmt": "echo 'Format not set up yet'",
    "test": "npm run build && npm run testOnly",
    "testOnly": "node --import tsx ./util/testrunner.ts"
  },
  "author": "Doridian",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.2",
    "glob": "^10.3.10",
    "tsx": "^4.6.2",
    "typescript": "^5.3.2"
  }
}
